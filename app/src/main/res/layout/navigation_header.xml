<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="220dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="#7B27F0">

    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_marginTop="5dp"
        android:id="@+id/profile_image"
        android:layout_width="150dp"
        android:layout_height="150dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginBottom="30dp"
        app:civ_border_width="2dp"
        android:background="#fff"/>

    <TextView
        android:id="@+id/facebook_profile_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/profile_image"
        app:layout_constraintLeft_toLeftOf="@id/profile_image"
        app:layout_constraintRight_toRightOf="@id/profile_image"
        android:text="facebook title:"
        android:layout_marginTop="10dp"
        android:textColor="#fff"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/facebook_profile_title"
        app:layout_constraintLeft_toLeftOf="@id/facebook_profile_title"
        app:layout_constraintRight_toRightOf="@id/facebook_profile_title"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="Firebase title: "
        android:textStyle="bold"
        android:textColor="#fff"/>

</android.support.constraint.ConstraintLayout>